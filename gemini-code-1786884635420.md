====================================================================
               HEALTHCARE APPLICATION - FULL PROJECT CODE
====================================================================

--------------------------------------------------------------------
FILE 1: index.html
--------------------------------------------------------------------
<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>CarePulse - Medical Appointments & AI Assistant</title>
  <link rel="stylesheet" href="style.css">
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/@tabler/icons-webfont@latest/tabler-icons.min.css">
</head>
<body>
  <!-- Navigation -->
  <nav>
    <a href="#" class="logo">
      <div class="logo-icon"><i class="ti ti-heart-handshake"></i></div>
      CarePulse
    </a>
    <ul class="nav-links">
      <li><a href="#doctors">Find Doctors</a></li>
      <li><a href="#specialties">Specialties</a></li>
      <li><a href="#about">About Us</a></li>
    </ul>
    <div class="nav-actions">
      <a href="auth.html" class="btn-outline">Log In</a>
      <a href="auth.html" class="btn-primary">Register</a>
    </div>
  </nav>

  <!-- Hero Section -->
  <section class="hero">
    <span class="hero-badge">✨ Healthcare Reimagined</span>
    <h1>Your Health Journey,<br>Simplified & Intelligent.</h1>
    <p>Book top-rated specialists near you and get instant guidance from our AI health assistant 24/7.</p>
    <div class="hero-cta">
      <a href="#doctors" class="btn-hero btn-hero-white">Find a Doctor</a>
      <button class="btn-hero btn-hero-ghost" onclick="openAiChat()">Talk to AI</button>
    </div>
  </section>

  <!-- Stats Bar -->
  <div class="stats-bar">
    <div><div class="stat-num">500+</div><div class="stat-label">Verified Doctors</div></div>
    <div><div class="stat-num">4.9/5</div><div class="stat-label">Patient Rating</div></div>
    <div><div class="stat-num">24/7</div><div class="stat-label">AI Availability</div></div>
    <div><div class="stat-num">50k+</div><div class="stat-label">Appointments</div></div>
  </div>

  <main>
    <!-- Search Section -->
    <div class="search-card" id="doctors">
      <h3>Search Top Specialists</h3>
      <div class="search-grid">
        <div class="field">
          <label>Doctor Name or Condition</label>
          <input type="text" id="searchInput" placeholder="e.g. Dr. Sharma or Cardiology">
        </div>
        <div class="field">
          <label>Specialty</label>
          <select id="specSelect">
            <option value="">All Specialties</option>
            <option value="Cardiology">Cardiology</option>
            <option value="Dermatology">Dermatology</option>
            <option value="Neurology">Neurology</option>
            <option value="Pediatrics">Pediatrics</option>
          </select>
        </div>
        <div class="field">
          <label>City</label>
          <input type="text" id="cityInput" placeholder="Enter City">
        </div>
        <button class="btn-search" onclick="filterDoctors()">Search</button>
      </div>
    </div>

    <!-- Doctors Cards Container -->
    <div class="doctors-grid" id="doctorsContainer"></div>
  </main>

  <!-- AI Chat Floating Button & Modal -->
  <button class="ai-fab" onclick="openAiChat()"><i class="ti ti-sparkles"></i> AI Health Assistant</button>

  <div class="modal-overlay" id="aiModal">
    <div class="modal ai-modal">
      <div class="modal-header">
        <h3>CarePulse AI Assistant</h3>
        <button class="modal-close" onclick="closeAiChat()">&times;</button>
      </div>
      <div class="ai-chat-body" id="chatBody"></div>
      <div class="ai-input-row">
        <input type="text" id="aiInput" class="ai-input" placeholder="Ask about symptoms...">
        <button class="ai-send-btn" onclick="sendAiMessage()">Send</button>
      </div>
    </div>
  </div>

  <script src="script.js"></script>
</body>
</html>

--------------------------------------------------------------------
FILE 2: auth.html
--------------------------------------------------------------------
<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>CarePulse - Authentication</title>
  <link rel="stylesheet" href="auth.css">
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/@tabler/icons-webfont@latest/tabler-icons.min.css">
</head>
<body>
  <nav>
    <a href="index.html" class="logo"><div class="logo-icon"><i class="ti ti-heart-handshake"></i></div> CarePulse</a>
    <a href="index.html" class="nav-back"><i class="ti ti-arrow-left"></i> Back to Home</a>
  </nav>

  <div class="auth-wrapper">
    <div class="auth-left">
      <h1 class="auth-tagline">Manage your health <span>efforts easily.</span></h1>
      <p class="auth-desc">Join thousands of patients booking instant doctor appointments daily.</p>
    </div>

    <div class="auth-card">
      <div class="auth-tabs">
        <button class="tab-btn active" onclick="switchTab('login')">Login</button>
        <button class="tab-btn" onclick="switchTab('register')">Register</button>
      </div>

      <!-- Login Panel -->
      <div class="auth-panel active" id="loginPanel">
        <h2>Welcome Back</h2>
        <div class="subtitle">Log in to manage appointments</div>
        <div class="form-field">
          <label>Email Address</label>
          <div class="input-wrap"><i class="ti ti-mail ti"></i><input type="email" id="loginEmail" placeholder="you@example.com"></div>
        </div>
        <div class="form-field">
          <label>Password</label>
          <div class="input-wrap"><i class="ti ti-lock ti"></i><input type="password" id="loginPassword" placeholder="••••••••"></div>
        </div>
        <button class="btn-primary-full" onclick="handleLogin()">Sign In</button>
      </div>

      <!-- Register Panel -->
      <div class="auth-panel" id="registerPanel">
        <h2>Create Account</h2>
        <div class="subtitle">Get started with CarePulse</div>
        <div class="form-field">
          <label>Full Name</label>
          <div class="input-wrap"><i class="ti ti-user ti"></i><input type="text" id="regName" placeholder="John Doe"></div>
        </div>
        <div class="form-field">
          <label>Email Address</label>
          <div class="input-wrap"><i class="ti ti-mail ti"></i><input type="email" id="regEmail" placeholder="you@example.com"></div>
        </div>
        <div class="form-field">
          <label>Password</label>
          <div class="input-wrap"><i class="ti ti-lock ti"></i><input type="password" id="regPassword" placeholder="••••••••"></div>
        </div>
        <button class="btn-primary-full" onclick="handleRegister()">Create Account</button>
      </div>
    </div>
  </div>
  <script src="auth.js"></script>
</body>
</html>

--------------------------------------------------------------------
FILE 3: server.js
--------------------------------------------------------------------
const express = require('express');
const mongoose = require('mongoose');
const cors = require('cors');
const bcrypt = require('bcryptjs');
const jwt = require('jsonwebtoken');
require('dotenv').config();

const app = express();
app.use(express.json());
app.use(cors());

// Database Schema
const userSchema = new mongoose.Schema({
  name: String,
  email: { type: String, unique: true, required: true },
  password: { type: String, required: true }
});

const User = mongoose.model('User', userSchema);

// Auth Routes
app.post('/api/register', async (req, res) => {
  try {
    const { name, email, password } = req.body;
    const hashedPassword = await bcrypt.hash(password, 10);
    const user = await User.create({ name, email, password: hashedPassword });
    res.status(201).json({ message: "User registered successfully", userId: user._id });
  } catch (err) {
    res.status(400).json({ error: "Registration failed or email already exists." });
  }
});

app.post('/api/login', async (req, res) => {
  try {
    const { email, password } = req.body;
    const user = await User.findOne({ email });
    if (!user) return res.status(404).json({ error: "User not found" });

    const isMatch = await bcrypt.compare(password, user.password);
    if (!isMatch) return res.status(400).json({ error: "Invalid credentials" });

    const token = jwt.sign({ id: user._id }, process.env.JWT_SECRET || 'secretKey', { expiresIn: '1h' });
    res.json({ token, user: { name: user.name, email: user.email } });
  } catch (err) {
    res.status(500).json({ error: "Server error" });
  }
});

const PORT = process.env.PORT || 5000;
app.listen(PORT, () => console.log(`Server running on port ${PORT}`));

--------------------------------------------------------------------
FILE 4: package.json
--------------------------------------------------------------------
{
  "name": "health-app",
  "version": "1.0.0",
  "description": "Healthcare Appointment Booking and AI Assistant Application",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js"
  },
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "cors": "^2.8.5",
    "dotenv": "^16.0.3",
    "express": "^4.18.2",
    "jsonwebtoken": "^9.0.0",
    "mongoose": "^7.0.0"
  },
  "devDependencies": {
    "nodemon": "^2.0.22"
  }
}

--------------------------------------------------------------------
FILE 5: .env
--------------------------------------------------------------------
PORT=5000
MONGO_URI=mongodb://localhost:27017/healthapp
JWT_SECRET=your_super_secret_jwt_key_here_12345
GEMINI_API_KEY=your_gemini_api_key_here